{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch Sandbox",
            "type": "cppvsdbg", // 使用 VS 的调试引擎
            "request": "launch",
            "program": "${workspaceRoot}/bin/Debug-windows-x86_64/Sandbox/Sandbox.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}/bin/Debug-windows-x86_64/Sandbox",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "Build Sandbox" // 关键：运行前先执行上面的编译任务
        }
    ]
}